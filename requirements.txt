# Core Python packages
beautifulsoup4
requests
python-docx
PyMuPDF

# Selenium and Chrome driver manager
selenium
webdriver-manager

# LangChain and related packages
langchain
langchain-community
langchain-huggingface

# Vector store and embeddings
faiss-cpu  # or faiss-gpu if using GPU, but faiss-cpu is usually safer on Streamlit Cloud
transformers
torch  # Required by transformers and HuggingFacePipeline
accelerate  # Optional, improves transformer model loading
sentence-transformers

# Streamlit itself
streamlit
